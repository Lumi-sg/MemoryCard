import "../styles/Header.css";

const Header = () => {
    return (
        <div className="Header">
            <h1>Divination Card Memory Game</h1>
            <p>Get points by clicking on a card but don't click on any more than once!</p>
        </div>
    );
};

export default Header;
